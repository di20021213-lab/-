playwright>=1.40
pyyaml>=6.0
python-dotenv>=1.0
requests[socks]>=2.31  # [socks] — поддержка socks5:// прокси
tzdata>=2024.1 ; sys_platform == 'win32'  # база часовых поясов: в Windows её нет в системе,
                                          # и zoneinfo без неё не находит Europe/Moscow
