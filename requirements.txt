requests>=2.28
# Доверие системному хранилищу сертификатов (корпоративные MITM-прокси).
truststore>=0.9
# Расшифровка HLS AES-128 (некоторые записи МТС Линк/вебинаров).
cryptography>=41
# Отрисовка водяного знака (иконка + текст).
pillow>=10
