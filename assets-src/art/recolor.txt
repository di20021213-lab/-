# Кому менять масть после снятия фона: «<id> <масть>», масти — в tools/recolor.py.
# Нужно, когда генератор не пропускает цвет: на лошадь подряд упали четыре
# формулировки, прошла только та, где цвета не было вовсе. Как и flip.txt,
# лежит в репозитории, а не в голове: иначе переимпорт всех пород разом молча
# возвращает исходную масть.
vladimir bay
